# Streamlit Cloud — dashboard runtime only.
# Models are pre-computed; no training dependencies needed.
# For local development with training, use: poetry install

numpy>=1.24.0
pandas>=2.0.0
plotly>=5.14.0
streamlit>=1.35.0
openai>=2.0.0
